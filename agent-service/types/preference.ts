/**
 * 偏好类型定义
 *
 * 用于偏好检测和注入功能
 */

/** 偏好类型 */
export type PreferenceType =
  | 'like'      // 喜欢
  | 'dislike'   // 不喜欢
  | 'want'      // 想要/需要
  | 'avoid'     // 避免/讨厌
  | 'habit'     // 习惯
  | 'style';    // 风格偏好
